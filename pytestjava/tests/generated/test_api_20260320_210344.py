import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent.parent))

import pytest
from utils.api_client import api_client

# Auto-generated test cases for API endpoints
# Generated by API Test Framework


# POSITIVE TEST CASES

@pytest.mark.positive
@pytest.mark.smoke
def test_brand_listall_positive():
    """测试 get /brand/listAll - 正向测试用例"""
    
    # 准备测试数据
    test_data = None
    
    # 发起API请求
    response = api_client.get('/brand/listAll', data=test_data)
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言响应结构
    assert 'requestId' in data
    assert 'brands' in data or isinstance(data, list)


@pytest.mark.positive
@pytest.mark.smoke
def test_brand_create_positive():
    """测试 post /brand/create - 正向测试用例"""
    
    # 准备测试数据
    test_data = {"name": "TestBrand", "firstLetter": "T", "sort": 0, "factoryStatus": 1, "showStatus": 1, "logo": "https://example.com/logo.jpg", "bigPic": "https://example.com/big.jpg", "brandStory": "This is a test brand story."}
    
    # 发起API请求
    response = api_client.post('/brand/create', data=test_data)
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言响应结构
    assert 'requestId' in data
    assert 'brands' in data or isinstance(data, list)


@pytest.mark.positive
@pytest.mark.smoke
def test_brand_update_id_positive():
    """测试 post /brand/update/1 - 正向测试用例"""
    
    # 准备测试数据
    test_data = {"name": "TestBrand", "firstLetter": "T", "sort": 0, "factoryStatus": 1, "showStatus": 1, "logo": "https://example.com/logo.jpg", "bigPic": "https://example.com/big.jpg", "brandStory": "This is a test brand story."}
    
    # 发起API请求
    response = api_client.post('/brand/update/1', data=test_data)
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言响应结构
    assert 'requestId' in data
    assert 'brands' in data or isinstance(data, list)


@pytest.mark.positive
@pytest.mark.smoke
def test_brand_delete_id_positive():
    """测试 get /brand/delete/1 - 正向测试用例"""
    
    # 准备测试数据
    test_data = None
    
    # 发起API请求
    response = api_client.get('/brand/delete/1', data=test_data)
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言响应结构
    assert 'requestId' in data
    assert 'brands' in data or isinstance(data, list)


@pytest.mark.positive
@pytest.mark.smoke
def test_brand_list_positive():
    """测试 get /brand/list - 正向测试用例"""
    
    # 准备测试数据
    test_data = None
    
    # 发起API请求
    response = api_client.get('/brand/list', data=test_data)
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言响应结构
    assert 'requestId' in data
    assert 'brands' in data or isinstance(data, list)


@pytest.mark.positive
@pytest.mark.smoke
def test_brand_id_positive():
    """测试 get /brand/1 - 正向测试用例"""
    
    # 准备测试数据
    test_data = None
    
    # 发起API请求
    response = api_client.get('/brand/1', data=test_data)
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言响应结构
    assert 'requestId' in data
    assert 'brands' in data or isinstance(data, list)


# NEGATIVE TEST CASES

@pytest.mark.negative
@pytest.mark.regression
def test_brand_listall_negative_unauthorized():
    """测试 get /brand/listAll - 负向测试用例: unauthorized"""
    
    # 准备无效的测试数据
    test_data = None
    
    # 发起API请求
    response = api_client.get('/brand/listAll', data=test_data)
    
    # 验证错误响应
    assert response.status_code == 401
    
    # 断言错误结构
    error_data = response.json()
    assert 'errorCode' in error_data
    assert 'message' in error_data


@pytest.mark.negative
@pytest.mark.regression
def test_brand_delete_id_negative_unauthorized():
    """测试 get /brand/delete/1 - 负向测试用例: unauthorized"""
    
    # 准备无效的测试数据
    test_data = None
    
    # 发起API请求
    response = api_client.get('/brand/delete/1', data=test_data)
    
    # 验证错误响应
    assert response.status_code == 401
    
    # 断言错误结构
    error_data = response.json()
    assert 'errorCode' in error_data
    assert 'message' in error_data


@pytest.mark.negative
@pytest.mark.regression
def test_brand_list_negative_unauthorized():
    """测试 get /brand/list - 负向测试用例: unauthorized"""
    
    # 准备无效的测试数据
    test_data = None
    
    # 发起API请求
    response = api_client.get('/brand/list', data=test_data)
    
    # 验证错误响应
    assert response.status_code == 401
    
    # 断言错误结构
    error_data = response.json()
    assert 'errorCode' in error_data
    assert 'message' in error_data


@pytest.mark.negative
@pytest.mark.regression
def test_brand_id_negative_unauthorized():
    """测试 get /brand/1 - 负向测试用例: unauthorized"""
    
    # 准备无效的测试数据
    test_data = None
    
    # 发起API请求
    response = api_client.get('/brand/1', data=test_data)
    
    # 验证错误响应
    assert response.status_code == 401
    
    # 断言错误结构
    error_data = response.json()
    assert 'errorCode' in error_data
    assert 'message' in error_data


# PERFORMANCE TEST CASES

@pytest.mark.performance
def test_brand_listall_performance():
    """测试 get /brand/listAll - 性能测试"""
    
    # 准备测试数据
    test_data = None
    
    # 测量响应时间
    import time
    start_time = time.time()
    
    response = api_client.get('/brand/listAll', data=test_data)
    
    end_time = time.time()
    response_time = end_time - start_time
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言性能要求
    assert response_time < 1.0, "响应时间超过限制"


@pytest.mark.performance
def test_brand_create_performance():
    """测试 post /brand/create - 性能测试"""
    
    # 准备测试数据
    test_data = {"name": "TestBrand", "firstLetter": "T", "sort": 0, "factoryStatus": 1, "showStatus": 1, "logo": "https://example.com/logo.jpg", "bigPic": "https://example.com/big.jpg", "brandStory": "This is a test brand story."}
    
    # 测量响应时间
    import time
    start_time = time.time()
    
    response = api_client.post('/brand/create', data=test_data)
    
    end_time = time.time()
    response_time = end_time - start_time
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言性能要求
    assert response_time < 1.0, "响应时间超过限制"


@pytest.mark.performance
def test_brand_update_id_performance():
    """测试 post /brand/update/1 - 性能测试"""
    
    # 准备测试数据
    test_data = {"name": "TestBrand", "firstLetter": "T", "sort": 0, "factoryStatus": 1, "showStatus": 1, "logo": "https://example.com/logo.jpg", "bigPic": "https://example.com/big.jpg", "brandStory": "This is a test brand story."}
    
    # 测量响应时间
    import time
    start_time = time.time()
    
    response = api_client.post('/brand/update/1', data=test_data)
    
    end_time = time.time()
    response_time = end_time - start_time
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言性能要求
    assert response_time < 1.0, "响应时间超过限制"


@pytest.mark.performance
def test_brand_delete_id_performance():
    """测试 get /brand/delete/1 - 性能测试"""
    
    # 准备测试数据
    test_data = None
    
    # 测量响应时间
    import time
    start_time = time.time()
    
    response = api_client.get('/brand/delete/1', data=test_data)
    
    end_time = time.time()
    response_time = end_time - start_time
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言性能要求
    assert response_time < 1.0, "响应时间超过限制"


@pytest.mark.performance
def test_brand_list_performance():
    """测试 get /brand/list - 性能测试"""
    
    # 准备测试数据
    test_data = None
    
    # 测量响应时间
    import time
    start_time = time.time()
    
    response = api_client.get('/brand/list', data=test_data)
    
    end_time = time.time()
    response_time = end_time - start_time
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言性能要求
    assert response_time < 1.0, "响应时间超过限制"


@pytest.mark.performance
def test_brand_id_performance():
    """测试 get /brand/1 - 性能测试"""
    
    # 准备测试数据
    test_data = None
    
    # 测量响应时间
    import time
    start_time = time.time()
    
    response = api_client.get('/brand/1', data=test_data)
    
    end_time = time.time()
    response_time = end_time - start_time
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言性能要求
    assert response_time < 1.0, "响应时间超过限制"

