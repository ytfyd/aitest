import pytest
import sys
import json
import os
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent.parent.parent))

from utils.api_client import api_client


# Response storage file
RESPONSE_FILE = Path(__file__).parent.parent.parent / "test-reports" / "responses.json"


def save_response(test_name: str, response_data: dict):
    """Save response data to file for reporting"""
    try:
        RESPONSE_FILE.parent.mkdir(parents=True, exist_ok=True)
        
        responses = {}
        if RESPONSE_FILE.exists():
            with open(RESPONSE_FILE, 'r', encoding='utf-8') as f:
                responses = json.load(f)
        
        responses[test_name] = response_data
        
        with open(RESPONSE_FILE, 'w', encoding='utf-8') as f:
            json.dump(responses, f, ensure_ascii=False, indent=2)
    except Exception as e:
        print(f"Warning: Failed to save response: {e}")


# Authentication fixture - login before running tests
@pytest.fixture(scope="session", autouse=True)
def setup_authentication():
    """Auto-login before running tests"""
    # Clear previous responses
    if RESPONSE_FILE.exists():
        os.remove(RESPONSE_FILE)
    
    api_client.login()
    yield
    api_client.clear_auth()



# Auto-generated test cases for API endpoints
# Generated by API Test Framework


# POSITIVE TEST CASES

@pytest.mark.positive
@pytest.mark.smoke
def test_list_positive():
    """测试 get /list - 正向测试用例"""
    
    # 准备测试数据
    test_data = {}
    
    # 发起API请求
    response = api_client.get('/list')
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_list_positive', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {}
    })
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言响应结构 - 检查业务响应码
    assert data.get('code') == 200, f"业务响应码错误: {data.get('msg', 'Unknown error')}"
    


@pytest.mark.positive
@pytest.mark.smoke
def test_captchaimage_positive():
    """测试 get /captchaImage - 正向测试用例"""
    
    # 准备测试数据
    test_data = {}
    
    # 发起API请求
    response = api_client.get('/captchaImage')
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_captchaimage_positive', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {}
    })
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言响应结构 - 检查业务响应码
    assert data.get('code') == 200, f"业务响应码错误: {data.get('msg', 'Unknown error')}"
    


@pytest.mark.positive
@pytest.mark.smoke
def test_treeselect_positive():
    """测试 get /treeselect - 正向测试用例"""
    
    # 准备测试数据
    test_data = {}
    
    # 发起API请求
    response = api_client.get('/treeselect')
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_treeselect_positive', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {}
    })
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言响应结构 - 检查业务响应码
    assert data.get('code') == 200, f"业务响应码错误: {data.get('msg', 'Unknown error')}"
    


@pytest.mark.positive
@pytest.mark.smoke
def test_system_menu_positive():
    """测试 post /system/menu - 正向测试用例"""
    
    # 准备测试数据
    test_data = {"menuName": "test_name", "menuType": "test_type", "orderNum": 1}
    
    # 发起API请求
    response = api_client.post('/system/menu', data={"menuName": "test_name", "menuType": "test_type", "orderNum": 1})
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_system_menu_positive', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {"menuName": "test_name", "menuType": "test_type", "orderNum": 1}
    })
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言响应结构 - 检查业务响应码
    assert data.get('code') == 200, f"业务响应码错误: {data.get('msg', 'Unknown error')}"
    


@pytest.mark.positive
@pytest.mark.smoke
def test_rolemenutreeselect_roleid_positive():
    """测试 get /roleMenuTreeselect/{roleId} - 正向测试用例"""
    
    # 准备测试数据
    test_data = {}
    
    # 发起API请求
    response = api_client.get('/roleMenuTreeselect/{roleId}')
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_rolemenutreeselect_roleid_positive', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {}
    })
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言响应结构 - 检查业务响应码
    assert data.get('code') == 200, f"业务响应码错误: {data.get('msg', 'Unknown error')}"
    


@pytest.mark.positive
@pytest.mark.smoke
def test__positive():
    """测试 delete / - 正向测试用例"""
    
    # 准备测试数据
    test_data = {}
    
    # 发起API请求
    response = api_client.delete('/')
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test__positive', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {}
    })
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言响应结构 - 检查业务响应码
    assert data.get('code') == 200, f"业务响应码错误: {data.get('msg', 'Unknown error')}"
    


@pytest.mark.positive
@pytest.mark.smoke
def test_page_positive():
    """测试 get /page - 正向测试用例"""
    
    # 准备测试数据
    test_data = {}
    
    # 发起API请求
    response = api_client.get('/page')
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_page_positive', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {}
    })
    
    # 验证响应
    data = api_client.validate_response(response, 200)
    
    # 断言响应结构 - 检查业务响应码
    assert data.get('code') == 200, f"业务响应码错误: {data.get('msg', 'Unknown error')}"
    


# NEGATIVE TEST CASES

@pytest.mark.negative
@pytest.mark.regression
def test_list_negative_invalid_params():
    """测试 get /list - 负向测试用例: invalid_params"""
    
    # 准备无效的测试数据
    test_data = {"invalid_param": "invalid_value"}
    
    # 发起API请求
    response = api_client.get('/list', data=test_data)
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_list_negative_invalid_params', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {"invalid_param": "invalid_value"}
    })
    
    # 验证错误响应
    assert response.status_code == 400
    
    # 断言错误结构
    error_data = response.json()
    assert 'errorCode' in error_data
    assert 'message' in error_data


@pytest.mark.negative
@pytest.mark.regression
def test_captchaimage_negative_invalid_params():
    """测试 get /captchaImage - 负向测试用例: invalid_params"""
    
    # 准备无效的测试数据
    test_data = {"invalid_param": "invalid_value"}
    
    # 发起API请求
    response = api_client.get('/captchaImage', data=test_data)
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_captchaimage_negative_invalid_params', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {"invalid_param": "invalid_value"}
    })
    
    # 验证错误响应
    assert response.status_code == 400
    
    # 断言错误结构
    error_data = response.json()
    assert 'errorCode' in error_data
    assert 'message' in error_data


@pytest.mark.negative
@pytest.mark.regression
def test_treeselect_negative_invalid_params():
    """测试 get /treeselect - 负向测试用例: invalid_params"""
    
    # 准备无效的测试数据
    test_data = {"invalid_param": "invalid_value"}
    
    # 发起API请求
    response = api_client.get('/treeselect', data=test_data)
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_treeselect_negative_invalid_params', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {"invalid_param": "invalid_value"}
    })
    
    # 验证错误响应
    assert response.status_code == 400
    
    # 断言错误结构
    error_data = response.json()
    assert 'errorCode' in error_data
    assert 'message' in error_data


@pytest.mark.negative
@pytest.mark.regression
def test_system_menu_negative_missing_required_fields():
    """测试 post /system/menu - 负向测试用例: missing_required_fields"""
    
    # 准备无效的测试数据
    test_data = {}
    
    # 发起API请求
    response = api_client.post('/system/menu', data=test_data)
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_system_menu_negative_missing_required_fields', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {}
    })
    
    # 验证错误响应
    assert response.status_code == 400
    
    # 断言错误结构
    error_data = response.json()
    assert 'errorCode' in error_data
    assert 'message' in error_data


@pytest.mark.negative
@pytest.mark.regression
def test_rolemenutreeselect_roleid_negative_invalid_params():
    """测试 get /roleMenuTreeselect/{roleId} - 负向测试用例: invalid_params"""
    
    # 准备无效的测试数据
    test_data = {"invalid_param": "invalid_value"}
    
    # 发起API请求
    response = api_client.get('/roleMenuTreeselect/{roleId}', data=test_data)
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_rolemenutreeselect_roleid_negative_invalid_params', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {"invalid_param": "invalid_value"}
    })
    
    # 验证错误响应
    assert response.status_code == 400
    
    # 断言错误结构
    error_data = response.json()
    assert 'errorCode' in error_data
    assert 'message' in error_data


@pytest.mark.negative
@pytest.mark.regression
def test_rolemenutreeselect_roleid_negative_invalid_id():
    """测试 get /roleMenuTreeselect/{roleId} - 负向测试用例: invalid_id"""
    
    # 准备无效的测试数据
    test_data = {}
    
    # 发起API请求
    response = api_client.get('/roleMenuTreeselect/{roleId}', data=test_data)
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_rolemenutreeselect_roleid_negative_invalid_id', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {}
    })
    
    # 验证错误响应
    assert response.status_code == 404
    
    # 断言错误结构
    error_data = response.json()
    assert 'errorCode' in error_data
    assert 'message' in error_data


@pytest.mark.negative
@pytest.mark.regression
def test__negative_unauthorized():
    """测试 delete / - 负向测试用例: unauthorized"""
    
    # 准备无效的测试数据
    test_data = {}
    
    # 发起API请求
    response = api_client.delete('/', data=test_data)
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test__negative_unauthorized', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {}
    })
    
    # 验证错误响应
    assert response.status_code == 403
    
    # 断言错误结构
    error_data = response.json()
    assert 'errorCode' in error_data
    assert 'message' in error_data


@pytest.mark.negative
@pytest.mark.regression
def test_page_negative_invalid_params():
    """测试 get /page - 负向测试用例: invalid_params"""
    
    # 准备无效的测试数据
    test_data = {"invalid_param": "invalid_value"}
    
    # 发起API请求
    response = api_client.get('/page', data=test_data)
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_page_negative_invalid_params', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {"invalid_param": "invalid_value"}
    })
    
    # 验证错误响应
    assert response.status_code == 400
    
    # 断言错误结构
    error_data = response.json()
    assert 'errorCode' in error_data
    assert 'message' in error_data


@pytest.mark.negative
@pytest.mark.regression
def test__negative_invalid_params():
    """测试 get / - 负向测试用例: invalid_params"""
    
    # 准备无效的测试数据
    test_data = {"invalid_param": "invalid_value"}
    
    # 发起API请求
    response = api_client.get('/', data=test_data)
    
    # 存储响应结果用于报告
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test__negative_invalid_params', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {"invalid_param": "invalid_value"}
    })
    
    # 验证错误响应
    assert response.status_code == 400
    
    # 断言错误结构
    error_data = response.json()
    assert 'errorCode' in error_data
    assert 'message' in error_data


# PERFORMANCE TEST CASES

@pytest.mark.performance
@pytest.mark.slow
def test_list_performance():
    """测试 get /list - 性能测试用例"""
    import time
    import statistics
    
    # 准备测试数据
    test_data = {}
    
    # 性能测试配置
    num_requests = 10  # 请求数量
    max_response_time = 2.0  # 最大响应时间（秒）
    avg_response_time = 1.0  # 平均响应时间（秒）
    
    # 记录响应时间
    response_times = []
    
    for i in range(num_requests):
        start_time = time.time()
        
        # 发起API请求
        response = api_client.get('/list')
        
        end_time = time.time()
        response_time = end_time - start_time
        response_times.append(response_time)
        
        # 验证响应
        assert response.status_code == 200
        data = response.json()
        assert data.get('code') == 200
    
    # 计算性能指标
    avg_time = statistics.mean(response_times)
    max_time = max(response_times)
    min_time = min(response_times)
    p95_time = statistics.quantiles(response_times, n=20)[18]  # 95th percentile
    
    # 存储性能数据
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_list_performance', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {},
        'avg_response_time': avg_time,
        'max_response_time': max_time,
        'min_response_time': min_time,
        'p95_response_time': p95_time,
        'num_requests': num_requests
    })
    
    # 验证性能指标
    assert avg_time <= avg_response_time, f"平均响应时间 {avg_time:.3f}s 超过阈值 {avg_response_time}s"
    assert max_time <= max_response_time, f"最大响应时间 {max_time:.3f}s 超过阈值 {max_response_time}s"
    
    # 打印性能报告
    print(f"性能测试报告:")
    print(f"  请求数量: {num_requests}")
    print(f"  平均响应时间: {avg_time:.3f}s")
    print(f"  最大响应时间: {max_time:.3f}s")
    print(f"  最小响应时间: {min_time:.3f}s")
    print(f"  P95响应时间: {p95_time:.3f}s")


@pytest.mark.performance
@pytest.mark.slow
def test_captchaimage_performance():
    """测试 get /captchaImage - 性能测试用例"""
    import time
    import statistics
    
    # 准备测试数据
    test_data = {}
    
    # 性能测试配置
    num_requests = 10  # 请求数量
    max_response_time = 2.0  # 最大响应时间（秒）
    avg_response_time = 1.0  # 平均响应时间（秒）
    
    # 记录响应时间
    response_times = []
    
    for i in range(num_requests):
        start_time = time.time()
        
        # 发起API请求
        response = api_client.get('/captchaImage')
        
        end_time = time.time()
        response_time = end_time - start_time
        response_times.append(response_time)
        
        # 验证响应
        assert response.status_code == 200
        data = response.json()
        assert data.get('code') == 200
    
    # 计算性能指标
    avg_time = statistics.mean(response_times)
    max_time = max(response_times)
    min_time = min(response_times)
    p95_time = statistics.quantiles(response_times, n=20)[18]  # 95th percentile
    
    # 存储性能数据
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_captchaimage_performance', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {},
        'avg_response_time': avg_time,
        'max_response_time': max_time,
        'min_response_time': min_time,
        'p95_response_time': p95_time,
        'num_requests': num_requests
    })
    
    # 验证性能指标
    assert avg_time <= avg_response_time, f"平均响应时间 {avg_time:.3f}s 超过阈值 {avg_response_time}s"
    assert max_time <= max_response_time, f"最大响应时间 {max_time:.3f}s 超过阈值 {max_response_time}s"
    
    # 打印性能报告
    print(f"性能测试报告:")
    print(f"  请求数量: {num_requests}")
    print(f"  平均响应时间: {avg_time:.3f}s")
    print(f"  最大响应时间: {max_time:.3f}s")
    print(f"  最小响应时间: {min_time:.3f}s")
    print(f"  P95响应时间: {p95_time:.3f}s")


@pytest.mark.performance
@pytest.mark.slow
def test_treeselect_performance():
    """测试 get /treeselect - 性能测试用例"""
    import time
    import statistics
    
    # 准备测试数据
    test_data = {}
    
    # 性能测试配置
    num_requests = 10  # 请求数量
    max_response_time = 2.0  # 最大响应时间（秒）
    avg_response_time = 1.0  # 平均响应时间（秒）
    
    # 记录响应时间
    response_times = []
    
    for i in range(num_requests):
        start_time = time.time()
        
        # 发起API请求
        response = api_client.get('/treeselect')
        
        end_time = time.time()
        response_time = end_time - start_time
        response_times.append(response_time)
        
        # 验证响应
        assert response.status_code == 200
        data = response.json()
        assert data.get('code') == 200
    
    # 计算性能指标
    avg_time = statistics.mean(response_times)
    max_time = max(response_times)
    min_time = min(response_times)
    p95_time = statistics.quantiles(response_times, n=20)[18]  # 95th percentile
    
    # 存储性能数据
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_treeselect_performance', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {},
        'avg_response_time': avg_time,
        'max_response_time': max_time,
        'min_response_time': min_time,
        'p95_response_time': p95_time,
        'num_requests': num_requests
    })
    
    # 验证性能指标
    assert avg_time <= avg_response_time, f"平均响应时间 {avg_time:.3f}s 超过阈值 {avg_response_time}s"
    assert max_time <= max_response_time, f"最大响应时间 {max_time:.3f}s 超过阈值 {max_response_time}s"
    
    # 打印性能报告
    print(f"性能测试报告:")
    print(f"  请求数量: {num_requests}")
    print(f"  平均响应时间: {avg_time:.3f}s")
    print(f"  最大响应时间: {max_time:.3f}s")
    print(f"  最小响应时间: {min_time:.3f}s")
    print(f"  P95响应时间: {p95_time:.3f}s")


@pytest.mark.performance
@pytest.mark.slow
def test_system_menu_performance():
    """测试 post /system/menu - 性能测试用例"""
    import time
    import statistics
    
    # 准备测试数据
    test_data = {"menuName": "test_name", "menuType": "test_type", "orderNum": 1}
    
    # 性能测试配置
    num_requests = 10  # 请求数量
    max_response_time = 2.0  # 最大响应时间（秒）
    avg_response_time = 1.0  # 平均响应时间（秒）
    
    # 记录响应时间
    response_times = []
    
    for i in range(num_requests):
        start_time = time.time()
        
        # 发起API请求
        response = api_client.post('/system/menu', data={"menuName": "test_name", "menuType": "test_type", "orderNum": 1})
        
        end_time = time.time()
        response_time = end_time - start_time
        response_times.append(response_time)
        
        # 验证响应
        assert response.status_code == 200
        data = response.json()
        assert data.get('code') == 200
    
    # 计算性能指标
    avg_time = statistics.mean(response_times)
    max_time = max(response_times)
    min_time = min(response_times)
    p95_time = statistics.quantiles(response_times, n=20)[18]  # 95th percentile
    
    # 存储性能数据
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_system_menu_performance', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {"menuName": "test_name", "menuType": "test_type", "orderNum": 1},
        'avg_response_time': avg_time,
        'max_response_time': max_time,
        'min_response_time': min_time,
        'p95_response_time': p95_time,
        'num_requests': num_requests
    })
    
    # 验证性能指标
    assert avg_time <= avg_response_time, f"平均响应时间 {avg_time:.3f}s 超过阈值 {avg_response_time}s"
    assert max_time <= max_response_time, f"最大响应时间 {max_time:.3f}s 超过阈值 {max_response_time}s"
    
    # 打印性能报告
    print(f"性能测试报告:")
    print(f"  请求数量: {num_requests}")
    print(f"  平均响应时间: {avg_time:.3f}s")
    print(f"  最大响应时间: {max_time:.3f}s")
    print(f"  最小响应时间: {min_time:.3f}s")
    print(f"  P95响应时间: {p95_time:.3f}s")


@pytest.mark.performance
@pytest.mark.slow
def test_rolemenutreeselect_roleid_performance():
    """测试 get /roleMenuTreeselect/{roleId} - 性能测试用例"""
    import time
    import statistics
    
    # 准备测试数据
    test_data = {}
    
    # 性能测试配置
    num_requests = 10  # 请求数量
    max_response_time = 2.0  # 最大响应时间（秒）
    avg_response_time = 1.0  # 平均响应时间（秒）
    
    # 记录响应时间
    response_times = []
    
    for i in range(num_requests):
        start_time = time.time()
        
        # 发起API请求
        response = api_client.get('/roleMenuTreeselect/{roleId}')
        
        end_time = time.time()
        response_time = end_time - start_time
        response_times.append(response_time)
        
        # 验证响应
        assert response.status_code == 200
        data = response.json()
        assert data.get('code') == 200
    
    # 计算性能指标
    avg_time = statistics.mean(response_times)
    max_time = max(response_times)
    min_time = min(response_times)
    p95_time = statistics.quantiles(response_times, n=20)[18]  # 95th percentile
    
    # 存储性能数据
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_rolemenutreeselect_roleid_performance', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {},
        'avg_response_time': avg_time,
        'max_response_time': max_time,
        'min_response_time': min_time,
        'p95_response_time': p95_time,
        'num_requests': num_requests
    })
    
    # 验证性能指标
    assert avg_time <= avg_response_time, f"平均响应时间 {avg_time:.3f}s 超过阈值 {avg_response_time}s"
    assert max_time <= max_response_time, f"最大响应时间 {max_time:.3f}s 超过阈值 {max_response_time}s"
    
    # 打印性能报告
    print(f"性能测试报告:")
    print(f"  请求数量: {num_requests}")
    print(f"  平均响应时间: {avg_time:.3f}s")
    print(f"  最大响应时间: {max_time:.3f}s")
    print(f"  最小响应时间: {min_time:.3f}s")
    print(f"  P95响应时间: {p95_time:.3f}s")


@pytest.mark.performance
@pytest.mark.slow
def test__performance():
    """测试 delete / - 性能测试用例"""
    import time
    import statistics
    
    # 准备测试数据
    test_data = {}
    
    # 性能测试配置
    num_requests = 10  # 请求数量
    max_response_time = 2.0  # 最大响应时间（秒）
    avg_response_time = 1.0  # 平均响应时间（秒）
    
    # 记录响应时间
    response_times = []
    
    for i in range(num_requests):
        start_time = time.time()
        
        # 发起API请求
        response = api_client.delete('/')
        
        end_time = time.time()
        response_time = end_time - start_time
        response_times.append(response_time)
        
        # 验证响应
        assert response.status_code == 200
        data = response.json()
        assert data.get('code') == 200
    
    # 计算性能指标
    avg_time = statistics.mean(response_times)
    max_time = max(response_times)
    min_time = min(response_times)
    p95_time = statistics.quantiles(response_times, n=20)[18]  # 95th percentile
    
    # 存储性能数据
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test__performance', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {},
        'avg_response_time': avg_time,
        'max_response_time': max_time,
        'min_response_time': min_time,
        'p95_response_time': p95_time,
        'num_requests': num_requests
    })
    
    # 验证性能指标
    assert avg_time <= avg_response_time, f"平均响应时间 {avg_time:.3f}s 超过阈值 {avg_response_time}s"
    assert max_time <= max_response_time, f"最大响应时间 {max_time:.3f}s 超过阈值 {max_response_time}s"
    
    # 打印性能报告
    print(f"性能测试报告:")
    print(f"  请求数量: {num_requests}")
    print(f"  平均响应时间: {avg_time:.3f}s")
    print(f"  最大响应时间: {max_time:.3f}s")
    print(f"  最小响应时间: {min_time:.3f}s")
    print(f"  P95响应时间: {p95_time:.3f}s")


@pytest.mark.performance
@pytest.mark.slow
def test_page_performance():
    """测试 get /page - 性能测试用例"""
    import time
    import statistics
    
    # 准备测试数据
    test_data = {}
    
    # 性能测试配置
    num_requests = 10  # 请求数量
    max_response_time = 2.0  # 最大响应时间（秒）
    avg_response_time = 1.0  # 平均响应时间（秒）
    
    # 记录响应时间
    response_times = []
    
    for i in range(num_requests):
        start_time = time.time()
        
        # 发起API请求
        response = api_client.get('/page')
        
        end_time = time.time()
        response_time = end_time - start_time
        response_times.append(response_time)
        
        # 验证响应
        assert response.status_code == 200
        data = response.json()
        assert data.get('code') == 200
    
    # 计算性能指标
    avg_time = statistics.mean(response_times)
    max_time = max(response_times)
    min_time = min(response_times)
    p95_time = statistics.quantiles(response_times, n=20)[18]  # 95th percentile
    
    # 存储性能数据
    try:
        resp_json = response.json()
    except:
        resp_json = {"raw_text": response.text}
    save_response('test_page_performance', {
        'status_code': response.status_code,
        'response': resp_json,
        'request_params': {},
        'avg_response_time': avg_time,
        'max_response_time': max_time,
        'min_response_time': min_time,
        'p95_response_time': p95_time,
        'num_requests': num_requests
    })
    
    # 验证性能指标
    assert avg_time <= avg_response_time, f"平均响应时间 {avg_time:.3f}s 超过阈值 {avg_response_time}s"
    assert max_time <= max_response_time, f"最大响应时间 {max_time:.3f}s 超过阈值 {max_response_time}s"
    
    # 打印性能报告
    print(f"性能测试报告:")
    print(f"  请求数量: {num_requests}")
    print(f"  平均响应时间: {avg_time:.3f}s")
    print(f"  最大响应时间: {max_time:.3f}s")
    print(f"  最小响应时间: {min_time:.3f}s")
    print(f"  P95响应时间: {p95_time:.3f}s")

